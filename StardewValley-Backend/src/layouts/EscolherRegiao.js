export default EscolherRegiao = () => {
    let numRegioes = 2
    console.log(
        `${regioes.forEach(element => {
            console.log(`${numAcao}. ${element}`)
            numRegioes++
        })}
    `)
}