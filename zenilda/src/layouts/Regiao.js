export default LocalFechado = () => {
    let numAcao = 2
    console.log(
        `1. Ir para outra região
        ${acoes.forEach(element => {
            console.log(`${numAcao}. ${element}`)
            numAcao++
        })}
`)
}

